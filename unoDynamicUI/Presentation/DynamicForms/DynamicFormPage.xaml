<Page x:Class="unoDynamicUI.Presentation.DynamicForms.DynamicFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <utu:NavigationBar Content="Dynamic Form Page" />

    <ContentControl x:Name="FormHost"
            Grid.Row="1" />

    <TextBlock Grid.Row="2"
         Margin="16,8,16,0"
         Text="{Binding LastAction}" />

    <TextBox Grid.Row="3"
       Margin="16,8,16,16"
       Text="{Binding LastSavedValues, Mode=OneWay}"
       IsReadOnly="True"
       AcceptsReturn="True"
       TextWrapping="Wrap"
       MinHeight="120" />
  </Grid>
</Page>
